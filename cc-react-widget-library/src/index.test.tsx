import { SettingsProvider, ThemingProvider } from '.'

describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(SettingsProvider).toBeTruthy()
    expect(ThemingProvider).toBeTruthy()
  })
})
